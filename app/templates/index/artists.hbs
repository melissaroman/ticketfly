<div id="results">
  <ul>
    Search Results for {{artistName}}:
    {{#each model.artists as |artist|}}
      <li>{{#link-to 'index.related'}}{{artist.name}}{{/link-to}}</li>
    {{/each}}
  </ul>
</div>
