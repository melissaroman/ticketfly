<div class="results">
  <ul class="artist-results">
    Search Results for {{artistName}}:
    {{#each model.artists as |artist|}}
      <li class="search-results"> {{#link-to 'index.related' (query-params id=artist.id)}}{{artist.name}}{{/link-to}}</li>
    {{/each}}
  </ul>
</div>
